<!DOCTYPE html>
<html>
  <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="style/style.css">
  <link href="https://fonts.googleapis.com/css?family=Roboto+Condensed:400,700" rel="stylesheet">
  <link rel="icon" type="image/png" href="favicon.ico">
  <title>OZPICS</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  <nav class="navbar navbar-light bg-light" style= "<%= seller_signed_in? ? "background-color: black !important;" : "background-color: white !important;"%>" >
      <div class="container">
        <%# logo %> 
        <div class="navbar-header">
          <%= link_to image_tag("OZPICS-LOGO.svg", alt: "logo"), root_path, class: "navbar-brand" %>
        </div>
        <%# search bar %>
        <%= form_tag(search_page_path, :method => "get", class: 'navbar-form navbar-left') do %>  
          <div class="input-group">  
            <%= search_field_tag :search, params[:search], placeholder: "Search Photographer", class: "form-control" %>  
            <div class="input-group-btn">  
              <%= button_tag "S", :class => 'btn btn-info glyphicon glyphicon-search',:name => nil%>  
            </div>  
          </div>  
        <% end %>
        <%# nav menu %>
        <div class="nav navbar-nav navbar-right">

            <% if seller_signed_in? %>
              <li>Hi <%= current_seller.firstname.capitalize %></li>
              <li class="nav-item dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                <div class="button">
                  <span class="caret"><%= link_to 'Dashboard', seller_page_path(current_seller) %></span>   
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <%= link_to 'Edit Account',  edit_seller_registration_path %>
                  <%= link_to 'Log out', destroy_seller_session_path, method: :delete %>
                </div>
                </div>         
              </li>

            <% elsif buyer_signed_in? %>

              <%= link_to 'Cart', line_items_path(buyer_id: current_buyer.id) %>

                <li class="nav-item dropdown">
                  <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                    <div class="button">
                      <span class="caret"><%= link_to 'Dashboard', orders_path %></span> 
                    </div>
                  </a>
                  
                <div class="dropdown-menu">
                  <%= link_to 'Edit Account', edit_buyer_registration_path %>
                  <%= link_to 'Log out', destroy_buyer_session_path, method: :delete %>
                </div>
              </div>

            <% else %>

              <div class="button">
                <%= link_to 'Join', join_path %>
              </div>

            <% end %>
        </div>
      </div>
    </nav>
      
    <%= yield %>

  </body>
</html>
